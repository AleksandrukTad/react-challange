import React, { ReactElement } from "react";
import { Container } from "@material-ui/core";

function HomePage(): ReactElement {
  return <Container maxWidth="sm">Login Successful</Container>;
}

export default HomePage;
